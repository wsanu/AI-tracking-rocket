# 测试与构建证据

此目录保存不属于源码的原始测试数据和历史构建记录。

- `test_records/`：可复核的结构化台架数据。
- `legacy_build_logs/`：整理前位于仓库根目录的旧构建日志；仅作问题追溯，不代表当前构建状态，默认不纳入版本控制。

新的测试记录应同时包含日期、硬件、固件 SHA-256、关键参数、安全条件和结论。大型飞行日志（如 `.ulg`）不提交到仓库，只在对应测试记录中写明外部存放位置和散列。
